// Define the Address interface representing the address structure
export interface Address {
  description: string;
  street: string;
  number: string;
  complement: string;
  neighborhood: string;
  city: string;
  state: string;
}
